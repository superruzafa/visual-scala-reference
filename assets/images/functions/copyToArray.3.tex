\matrix (B) [collection] {
    \node (bi) {$a_1$}; &
    \node [elements between]; &
    \node (bi+l-1) {$a_l$}; &
    \node (bi+l) {$b_{l + 1}$}; &
    \node [elements after]; \\
};

\matrix (A) [collection, above=2 of bi.north west, anchor=south west] {
    \node (a1) {$a_1$}; &
    \node [elements between]; &
    \node (al) {$a_l$}; &
    \node [elements after]; \\
};

\braceflow[-.2]{(a1.south west)}{(al.south east)}
\coordinate (x) at (lastbrace);
\braceflow[.2]{(bi.north west)}{(bi+l-1.north east)}
\draw [flow ->] (x) -- (lastbrace);

\measure[-.5]{$l$}{bi.south west}{bi+l-1.south east}
\draw [flow ->] (last measure) -- +(0, -1)
    node [element, below] {$l$};

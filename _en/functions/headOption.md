---
name: headOption
---

# `headOption`

@include [signatures/headOption.md]

`headOption` returns the first element of this collection, wrapped with `Some`.

@include [figure.html source="images/headOption.svg" desc="Diagram of the function headOption"]

On empty collections when there is no first element this function returns `None`.

@include [figure.html source="images/headOption.2.svg" desc="Diagram of the function headOption"]

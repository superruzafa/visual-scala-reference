---
---

\matrix [collection] {
    \node (k1) {$k_1$}; \\
    \node (k2) {$k_2$}; \\
    \node [vertical elements between]; \\
    \node (kn) {$k_n$}; \\
};

\foreach \i in {1,2,n}{
    \draw [key to value] (k\i.east) to +(1, 0) node (v) [element, right] {$v_{\i}$};
    \node (v\i) [element, right=3 of v] {$v_{\i}$};
}

\draw [<- flow, iterate up, next] (v1.west) arc;
\draw [flow ->, iterate down, next reversed] (v1.west) to (v2.west);
\draw [flow ->, dashed, iterate down] (v2.west) arc;
\draw [<- flow, dashed, iterate up, next] (vn.west) arc;

\coordinate (kc) at ($ (k1)!.5!(kn) $);
\coordinate (vc) at ($ (v1)!.5!(vn) $);
\coordinate (x) at ($ (kc)!.5!(vc) $);
\node at (x) [big arrow];
